// import { useEffect, useState } from "react";
// import { getColors } from "react-native-image-colors";

// export const useImageMainColor = (imageUri: string) => {
// 	const [colors, setColors] = useState(null);

// 	useEffect(() => {
// 		getColors(imageUri, {
// 			fallback: "#228B22",
// 			cache: true,
// 			key: imageUri,
// 		}).then(setColors);
// 	}, [imageUri]);

// 	return colors;
// };
